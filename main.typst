#import "content/max-likelihood-estimation.typst" as article

#set text(lang: article.meta.lang)

#document(
  article.meta.slug + "/index.html",
  title: article.meta.title,
)[
  #include "content/max-likelihood-estimation.typst"
]
